/**
 * IsLogged - Indicates whether a user account/session is currently logged in
 * @returns {boolean} True when logged in, false otherwise
 */
const IsLogged = () =>
{
    return false;
};

export default { IsLogged };
